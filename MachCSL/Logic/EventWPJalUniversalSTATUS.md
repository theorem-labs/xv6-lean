# Universal native per-hart JAL loop

`Machine/JalLoopUniversal{Fetch,}` and `Logic/EventWPJalUniversal{Spec,Proofs,Link}` complete the arbitrary-preboot generalization of the per-hart cycle and infinite loop. They supersede the concrete PMP snapshot premise of the earlier `EventWPJalLoop` checkpoint. The actual initial resources remain explicit; complete machine allocation, power/device composition and adequacy are separate obligations.

`UniversalFamily rs` is exactly `BootUniversal.StaticBoot jalImage.vector rs ∧ BootPmp.Off rs`. The former contains the thirteen static fields actually established by board/reset/firmware; the latter says all 64 PMP entries have A=OFF and L=0. `bootFacts_family` proves this conjunction for every hart of every actual `BootFacts jalImage` witness, including arbitrary preboot register files. No PMP address, unrelated PMP configuration bit, counter-inhibit/filter setting, pending bit, hardware pin value or initial counter value is fixed by this family. It is preserved by every actual JAL cycle clock choice and both PLIC pin-write arms.

`universal_fetch_plan` follows the generated model compositionally: PC/alignment/extension guards; Machine-mode translation; PMA selection and access checks; the actual PMP checker; MMIO classification; the real `read_ram` event; its split-width byte assembly; and the final fetch result. PMA's checked result is CannotSplit with granule exponent zero, so the actual split helper gives one four-byte access. The actual generated PMP checker visits 16 entries (while reset establishes OFF for all 64), preserving all configuration and current/previous address reads; its OFF proof makes no address assumption. Small kernel certificates establish only the non-PMP static helper computations. No event is erased, no alternate fetch implementation is installed, and no computed test result is trusted as a theorem.

The one RAM event retains its complete actual request and typed result. The concrete oracle admits exactly size four, JAL vector address, and word 0x6f; `EventWP.codeRamAccess` extracts and restores the four byte resources plus their pristine timestamp windows. In particular, the source's `read_kind_of_flags false false false` produces `Read_plain` even on this instruction-fetch path; the proof follows that actual access label. There is no whole-RAM ownership requirement or callee-WP oracle.

`universal_cycle_plan` combines that fetch with the already proved generated interrupt dispatch, decode/JAL execution, retirement and clock paths. Counter filters remain arbitrary, counters wrap modulo their real widths, every hardware-pin read branches independently, and both tick choices are retained. The native event fold proves the cycle WP one machine event at a time, including every allowed TSO read view. The symbolic register table describes only its 178 owned cells; its ignored two pin entries make no claim about current hardware pin values.

`universal_loop_wp` uses native guarded recursion and the actual restart rule. Each restart clears the real reservation, chooses either tick flag, and enters the cycle. Returned register/code ownership and the proved family closure discharge the next iteration; `resvFrag none` is retained and rewrapped as `resvAny`. The concrete `registryUniversalJalWPSpec` supplies all event/register/RAM/restart implementations at the common 23-slot UART/PLIC registry. `registry_boot_loop` directly specializes the infinite WP to an arbitrary actual boot witness, leaving only the concrete generation certificate, CPU-owned register cells, shared code resources and reservation custody for the boot allocator to provide.

This is a native infinite per-hart NotStuck WP for the real model, not a finite trace witness. It is still conditional on actual allocated resources, ghost names and the explicit platform instance, and does not by itself prove the all-actor machine gate, the paper's xv6 kernel theorem, or closed adequacy.

Validation: complete concrete link build passed (474 dependency jobs). Fresh physical-module-origin audit covered all 67 logical declarations in the five modules, including private helpers, with only `propext`, `Classical.choice`, and `Quot.sound`, no unsafe/partial logical dependency, and no runtime-companion exclusions. `universal_fetch_plan`, `bootFacts_family`, and `registryUniversalJalWPSpec` use only those three standard axioms. Audit source/log: `/tmp/xv6-lean-research/JalUniversalAudit.lean`, `jal-universal-audit.log`.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
