# Actual Bare fetch-bytes

Root-owned five modules Defs/Spec/Plan/Proofs/Link, frozen after native build.
`outer_boundary` proves actual outer fetch memory access; `fetch_boundary`
composes actual extension check, Bare translation, effective privilege and the
checked physical event. `wp_fetch_bytes` and `nativeSpec` supply the actual
native WP from one seven-cell fractional footprint and a context byte window.
Eleven real register reads precede the real plain read; the reused status and
privilege cells are sequentially owned once. Result bytes are arbitrary; all
successful tag and error-Exit residuals are retained in the proved boundary.

Explicit premises are Supervisor/SXL2/Bare, exact2-or4 alignment, TOR RAM,
matching executable PMA and disabled HTIF. Fetch imposes no MPRV restriction.
The same seven cells, running context, window and actual chosen-view receipt
reach the guarded continuation. No new camera, allocation or subordinate WP
oracle. This is a Bare fetch-bytes theorem; actual fetch PC selection and
compressed/base decoding, KPT translation and source whole-function WP remain
open. It must not be presented as a source supervisor function theorem.

Validation: `PATH=/home/jason/.elan/bin:$PATH python3 tools/lake.py build
MachCSL.Logic.SupervisorBareFetchLink` passed 527 jobs; fresh physical-origin
owner audit checks all 60 declarations and every opaque value/type/constructor,
standard three axioms only, zero exclusions. Independent review passed; see
docs/reviews/supervisor-bare-fetch-peer-review.md.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
