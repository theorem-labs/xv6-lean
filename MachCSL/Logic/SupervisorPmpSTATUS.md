# Native supervisor PMP ownership

The four Logic/SupervisorPmp modules connect the actual generated supervisor TOR grant to the native fractional register fold. The partial plan lists only pmpcfg_n and pmpaddr_n, with independently arbitrary shares. Its checked path preserves the three actual read events, early loop return and entire symbolic table. No ownership is asserted for unlisted table fields.

The source config predicate owns exactly those two full cells and the entry-zero TorRam facts. The root-PPN index is retained but unused, as in SmodePte.pmp_config. config_intro constructs it from the actual two cells; wp_check consumes the generation certificate/configuration and restores that same configuration to the genuine continuation WP. Spec/actual/nativeSpec separate the interface and implementation. No camera, invariant, startup condition or software callback is introduced.

The positive-width/RAM conditions, explicit Supervisor argument and exact supported access constructors remain in the public contract. This is a PMP subprogram WP, with translation, PMA, instruction fetch and full kernel function composition still open. Source mapping and independent review are in docs/reviews/supervisor-pmp-native-peer-review.md.

Validation: 441 build jobs passed. Independent full physical audit checked all 48 logical declarations, opaque bodies and constructors, standard three axioms only, zero exclusions and no unsafe/partial dependency. Evidence: /tmp/xv6-lean-research/SupervisorPmpNativePeerAudit.lean and supervisor-pmp-native-peer-audit.log.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
