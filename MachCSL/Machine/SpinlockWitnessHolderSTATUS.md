# Reachable operational holder on the seven-write schedule

`SpinlockWitnessHolder{Defs,Proofs}.lean` supplies the missing non-vacuity fact for the event-defined `SpinlockPool.Holds` window. The witness executes the actual CPU0 AMO commit and then retires the following ADDIW and non-taken BNE. At that point CPU0's actual expression is `.hart generation 0 (.pure ())` and its actual PC is `0x80000028`, instruction index 10 (the counter load). The counter load has not yet executed. The complete twelve-entry pool and the earlier CPU1 blocked exclusive read are retained.

`powerOn_holder` proves an actual `PoolSteps` execution from the sole power thread and any powered-off state to that exact boundary. `holder_to_final` proves a composable suffix through the two remaining counter-work cycles and the existing blocked-unlock/release/retry sequence. `through_holder` exposes both segments and their actual concatenation to the existing `SpinlockWitnessRelease.finalState`, with exactly the previous seven messages and final continuations. No instruction interval or previous witness definition was changed.

`annotated_holder` takes only the initial off/generation-zero facts. It applies `SpinlockPool.annotate_run` to the real prefix, obtains the actual CPU0 labeled occurrence from erasure and Shape, and applies `boundary_holds` to the checked PC at the completed-instruction boundary. It then lifts the actual suffix from this same annotated middle state using the proved Covers theorem. Its result includes both schedule lengths and observation lists, exact erased middle/final configurations, `ScheduledSteps` for each segment and their concatenation, both pool invariants, and `Holds middle 0`.

`concrete_holder devices` supplies the existing explicit initial state and platform with no proposition premise. Initial devices and durable contents remain arbitrary. No abstract PoolInv holder was manufactured, and no caller supplies a cursor, phase or preservation callback. The annotation is governed by the existing functional occurrence-indexed schedule semantics; this is not a claim of unique annotation for an erased trace without its scheduler choices.

The physical-PC argument is restricted to `.pure ()` instruction boundaries. The resulting `Holds` fact refers to the full operational post-commit/pre-unlock window. This particular schedule uses no clock ticks, no intermediate power cycle, and the previously selected TSO views; CPUs 2 through 7 remain unscheduled. The separately proved all-run safety and exclusion theorems are available for the coordinator's single final integration root. These modules do not assert an xv6 kernel or filesystem theorem.

Validation: `PATH=/data/jason/.elan/bin:$PATH python3 tools/lake.py build MachCSL.Machine.SpinlockWitnessHolderProofs` passes all 566 jobs; the final proof module checks in 1.9 seconds. No new instruction-evaluation certificate is needed: the new prefix/suffix reuse the existing checked cycle facts and the generic `cyclesRun_sound` / `pauseRun_sound` lifts. The state reassembly uses the proved abstract `writeBack_twice` identity.

The fresh physical-origin audit checks all 27 declarations in both modules with zero exclusions, including private helpers and complete opaque-body/type/constructor traversal (`info.value? (allowOpaque := true)`). Only `propext`, `Classical.choice` and `Quot.sound` occur; there is no unsafe or partial semantic dependency, new axiom, `sorry`, native decision procedure or unchecked execution premise.

Evidence: `/tmp/xv6-lean-research/SpinlockWitnessHolderAudit.lean`, `spinlock-witness-holder-audit.log`, and `spinlock-witness-holder-build.log`. This addresses the holder-inhabitation request in `/tmp/xv6-lean-research/fable-spinlock-gate-review.json`; the coordinator owns the combined final safety/exclusion/witness root.

*Authorship note: this was researched and written by an AI coding agent (OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is posted from this account.*
