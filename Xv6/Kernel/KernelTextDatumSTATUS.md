# RX text datum: frozen

All seven modules compile (687 jobs), with all 36 approved signatures unchanged:
nine pure, eighteen per-byte native and nine window contracts. Native constructors
and concrete existing-registry links are provided; no new camera is introduced.

The carrier preserves exact source RX map claims, physical text interval,
full-tier nonidentity mapping, raw bytes and discarded pristine timestamps.
Physical/window access returns same-value reassembly. Full-tier page-local
extraction has an explicit page bound; unrestricted four-byte splitting keeps
cross-page reads separate. Context conversion is discarded-only and does not
mint a pin. Immutable readback derives every-agent/every-view `ReadsBytes` from
actual same-era heap/TSO resources and returns those resources plus the original
text window unchanged.

Fresh full physical-origin audit: all 150 declarations, including private and
generated helpers, types, opaque bodies and datatype constructors. Only the
standard three foundational axioms occur; zero exclusions, no unsafe/partial
or Initial dependency. Seven kernel boundary checks pass, including the
nonidentity trampoline and split of a page-crossing four-byte instruction.

Source mapping and remaining scope: `docs/design/kernel-text-datum-boundary.md`.
Evidence under `/tmp/xv6-lean-research/`: `kernel-text-datum-build.log`,
`KernelTextDatumOwnerAudit.lean`, `kernel-text-datum-owner-audit.log`,
`KernelTextDatumChecks.lean`, `kernel-text-datum-checks.log`.

Actual translated fetch, complete source InstrBytes and initial code-map
resource allocation remain pending. Existing frozen modules and umbrellas
are unchanged. Complete independent review passed.

Complete independent coordinator review passed, including the exact source
text rules and a fresh full 150-declaration audit; see
docs/reviews/kernel-text-datum-peer-review.md.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
