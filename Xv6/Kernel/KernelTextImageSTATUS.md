# Kernel text image

Implemented and owner-audited: five Lean modules, six pure and seven native
contracts. Build858 jobs; strict audit127 physical declarations and all
types/opaque bodies/constructor dependencies passes with standard axioms
only and zero unsafe/partial dependencies or exclusions.

Exact source sparse lookup, finite23748-byte conjunction equivalence,
positive text-address bounds, real static-map attachment to existing
physical/pristine bytes, tier monotonicity, generic windows and all fourteen
native mycpu fetch windows are proved. The final return includes its actual
two successor bytes. Source gaps remain absent.

This establishes code resources conditionally from existing physical text;
boot allocation of the whole text, native entry establishment, the complete
function and all whole-system roots remain separate. No native fetch/decode
success is assumed or concluded by this resource-only producer.

Evidence: /tmp/xv6-lean-research/kernel-text-image-build.log and
kernel-text-image-owner-audit.log; KernelTextImageOwnerAudit.lean. Source
mapping and exact scope: docs/design/kernel-text-image-boundary.md.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
