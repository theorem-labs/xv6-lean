# Shared Sv39 fetch: implemented native boundary

FROZEN pending independent review. All fifteen approved contracts are proved:
Half Pure5+native1; full Pure5+resource3+native1. Sixteen modules build through
`Xv6.Kernel.KptFetchLink` (1,029 jobs, no warnings). Native Specs use actual
existing capacities and complete generated translation/read/fetch programs.

The public full theorem consumes source RX/pristine InstrBytes, seven actual
control cells, the shared KPT residue, reservation and guarded final
continuation. Both chunk translations and physical memory reads are discharged
internally. There is no translation-success, fetch/body WP, physical-word or
read oracle input. Full raw error trees remain in the factors; actual RX and
owned ADUE=1 discharge their impossibility on this route.

Source four-aligned RVC reads four bytes, merely-two-aligned RVC reads two,
and a base instruction across page offset4094 performs two independently
mapped translations. Eager PC/MISA/extension reads remain in checked plans.
Native composition retains all seven cells, each updated folded residue,
actual reservation changes, all translation/read receipts and InstrBytes.
SATP/TLB/PMP ownership is only inside the existing residue, not duplicated.

The 287-declaration physical-origin audit traverses types, all opaque bodies
and datatype constructors. All cones use only propext, Classical.choice and
Quot.sound, zero exclusions and no unsafe/partial/Initial dependency. Eleven
kernel fixtures pass, including distinct nonidentity physical pages and an
actual generated cross-page fetch plan. Evidence:
`/tmp/xv6-lean-research/{KptFetchOwnerAudit.lean,KptFetchChecks.lean}` and
`kpt-fetch-{build,owner-audit,checks}.log`.

The pre-proof ordinary-`++` halves signature elaborated with an unintended
truncation; it was never proved. Coordinator-approved correction uses explicit
`BitVec.append` and is kernel-proved, with a nonzero upper 16-bit fixture.
New physical concatenation also uses explicit append. No impossible original
contract is claimed as verified.

Source mapping and full file inventory: `docs/design/kpt-fetch-boundary.md`.
Actual boot resource allocation/reachability and whole translated
function/cycle remain later integration work. Existing Config retains explicit
pmaBoot, Supervisor/SXL2/HTIF-none and getter conditions. No new camera or
Initial constructor is used. Frozen neighboring files and umbrellas unchanged.

Coordinator final peer review also passed: all implementation files read and
a fresh full audit checked 287 physical declarations through types, opaque
values and constructors, with standard axioms only and zero exclusions.
The corresponding peer report is in docs/reviews.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
