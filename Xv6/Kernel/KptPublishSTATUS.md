# KPT publication: frozen

All seven modules compile (721 jobs). The coordinator approved the fourteen
Defs/Spec obligations before implementation; all signatures remain unchanged.
The prefix constructs PureSpec/Spec/AllocationSpec using actual native components,
with concrete registry links for both resource contracts.

Canonical self-membership covers all raw words and arbitrary byte offsets.
Actual full user-tier slots, lists, pages and trees become the existing
kernel-tier ownership. The exact inert tree, words, node claims, list order
and depth semantics are preserved. Child transformation is proved by depth
induction and a native persistent update-wand fold.

Generic drained folds require ownPub≤view plus the source's explicit
agent-zero view receipt. Final own-view publication derives that receipt only
from actual agent-zero identity and TSO authority. Boot forwarding preserves
individual byte floors/anchors with a log-length upper bound and no drain or
log-top view. It returns the exact boot-arm reading credential at allocation.

Both allocation proofs first transform the supplied actual full user tree at
depth two, then call actual KptShared allocation with the same tree, supplied
TreeSpec/map authority and existing unset names. They return the same full
heap metadata/TSO/context resources, shared invariant, snapshot, bound,
log receipt and per-hart credential; the own-view route also returns its
actual view receipt. No new camera, physical replacement or era is allocated.

Fresh full physical-origin audit: all 76 declarations across seven modules,
including private/generated helpers, types, opaque bodies and constructors;
only propext/Classical.choice/Quot.sound, zero exclusions, no unsafe/partial
or Initial semantic dependency. Six kernel edge checks also pass.

Source mapping: `docs/design/kpt-publish-boundary.md`. Evidence:

- `/tmp/xv6-lean-research/kpt-publish-build.log`
- `/tmp/xv6-lean-research/KptPublishOwnerAudit.lean`
- `/tmp/xv6-lean-research/kpt-publish-owner-audit.log`
- `/tmp/xv6-lean-research/KptPublishChecks.lean`
- `/tmp/xv6-lean-research/kpt-publish-checks.log`

Source boot execution, the publication-site instruction proof and complete
translated-function closure remain later integration obligations. No actual
initial tree ownership is claimed from pure TreeSpec alone. Frozen existing
modules and umbrellas are unchanged.

Independent coordinator review passes all seven modules and a fresh full
76-declaration audit. See docs/reviews/kpt-publish-peer-review.md.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
