# Indexed native KPT fetch: implemented wrapper

FROZEN pending independent review. All 16 approved contracts implemented in
six modules: Pure10, Resource5, native1. Final MycpuKptFetchLink build passes
1,068 jobs with no warnings. Native Specs use the existing RegimeShell
capacity and actual KptFetch implementation.

The input code bundle is fourteen native virtual RX/pristine windows whose
union is the actual 34-byte ELF-backed fetch footprint, including final 01 11.
The same 50-cell packet supplies seven fetch cells and retains its exact 43-cell
complement, source bit resources and software x0. All 31 native GPRs, including
pinned TP, remain untouched. SATP/TLB/PMP vectors belong solely to the separate
residue, which returns coherent after actual translation updates.

The full packet/code, exact traceReservation, all translation/read receipts
and arbitrary caller frame return under unchanged guardChunks. MsFacts is
obtained from native packet ownership to derive SXL; the remaining owned
Config supplies privilege/pmaBoot/HTIF-none/C/ADUE. No physical-word/window,
translation-success, fetch/body WP, read oracle or new allocator is input.

Pure result-to-decoder equality and the original snapshot certificate identify
MycpuDecode.decoded at each actual index. The native theorem executes fetch
only; it does not replace the needed native decoder proof from owned CSRs.
Whole body/cycle/function composition and source boot resource allocation
remain later integration obligations.

Fresh audit: 117 physical-origin declarations in all six modules, including
private helpers and complete type/opaque-body/constructor cones; standard
three axioms only, zero exclusions, no unsafe/partial or Initial dependency.
Eight kernel checks passed (GPR/TP and translation exclusion, overlapping 44
bytes/34-byte coverage, final actual JR fetch and decoder identity).
Evidence under `/tmp/xv6-lean-research/`:
`MycpuKptFetchOwnerAudit.lean`, `MycpuKptFetchChecks.lean`,
`mycpu-kpt-fetch-{build,owner-audit,checks}.log`.
Full source mapping: `docs/design/mycpu-kpt-fetch-boundary.md`.
Frozen neighbors and umbrellas unchanged.

Coordinator final peer review also passed: all implementation files read and
a fresh full audit checked 117 physical declarations through types, opaque
values and constructors, with standard axioms only and zero exclusions.
The corresponding peer report is in docs/reviews.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
