# Full-tier source mycpu wrapper

Implemented: five modules, two pure and one native function contract.
Final Link supplies actual MycpuKpt.nativeSpec and nativePureSpec alongside
native Entry/Text. Build1,201 jobs and strict owner audit of all32 physical
declarations and complete type/opaque/constructor cones pass, with standard
axioms only and zero unsafe/partial dependencies or exclusions. The exported
native contract has no component proof parameter. Independent review is
recorded separately.

The public Spec exposes only the exact full-tier disabled source input,
identity kernel_text, an explicit actual same-hart boot-PMA cell, arbitrary
frame and genuine post-return continuation. It restores the source13-key
callee-saved/value result and the full original free-stack count, text,
PMA cell and frame. No configuration, root, phase, code-window or success
oracle is an input. Boot reachability, inhabited native entry and all six
whole-system roots remain open.

Design: docs/design/mycpu-sconf-kpt-boundary.md.
Evidence: /tmp/xv6-lean-research/mycpu-sconf-kpt-{signatures,build,owner-audit}.log.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
