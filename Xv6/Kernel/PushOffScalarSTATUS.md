# push_off scalar bodies

FROZEN: native Link GREEN931 jobs. All twelve approved pure contracts and
one native body rule are constructed, with unchanged Defs/Spec signatures.
Six modules: PushOffScalarDefs, Spec, Pure, Plan, Proofs and Link.

Exact normalized generated bodies at indices 0,4,6,9,12,17,18,20,21,23:
actual register-computed branch, modular updates and ADDIW sign extension,
arbitrary-RA low-bit-cleared JR, real jump alignment checks and discarded
x0 link writes retaining their eager nextPC reads. Full entry-file equality
connects the actual RF updates to the software GPR map and control packet.

The native rule retains the same fifty-cell packet at the original actual
Bare/KPT Regime and the literal caller frame. Translation resources, native
mstatus bit ties and every unmodified GPR return without duplication. No
fetch/decode/cycle/function, enabled-SIE prologue, migration or source entry
inhabitation is claimed. The only WP premise is the genuine body continuation.
No new camera, frozen-family edit or umbrella change.

Strict owner audit: all 201 physical-origin declarations in all six modules;
private lookup with setExporting false, full types/opaque bodies/constructors;
only propext, Classical.choice and Quot.sound, no unsafe/partial dependency,
zero exclusions. Sixteen concrete checks passed, covering modular SP wrap,
ADDIW overflow/truncation, logical shift, mask, taken/untaken BEQ including a
nonzero high word, arbitrary low-bit JR targets, backward jump and preserved RA.

Evidence under /tmp/xv6-lean-research:
- push-off-scalar-native.log (931 jobs; Plan 1.9s, native Proofs 1.3s)
- PushOffScalarAudit.lean and push-off-scalar-audit.log (201 declarations)
- PushOffScalarChecks.lean and push-off-scalar-checks.log (16 checks)
- push-off-scalar-freeze.sha256 and push-off-scalar-source.sha256

Source/design mapping: docs/design/push-off-scalar-boundary.md.
Independent artifact_audit implementation review PASS: fresh931-job build,
strict201-declaration audit, all16 owner fixtures and five additional actual-plan
checks. Report: docs/reviews/push-off-scalar-peer-review.md. No corrections.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
