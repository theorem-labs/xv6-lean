# push_off stack bodies

FROZEN: native Link GREEN1,121 jobs. All20 approved contracts are constructed:
17 pure,2 native resource,1 native body; Defs/Spec signatures unchanged.

The six actual normalized size8 STORE/LOAD bodies cover source indices1/2/3
and14/15/16, RA/S0/S1 at SP+24/+16/+8. Full-entry GPR correspondence,
actual raw false/error tails and every operand/translation/memory subevent
remain. Only actual successful responses are derived from owned resources.

The same50-cell packet and original translation Regime return. KPT borrows7
cells and frames43, preserving the actual folded shared residue and native
translation/A-D/data guard nesting. Bare opens actual existential SATP/PMP
ownership to use10 cells plus43 framed, derives mode/TOR/RAM/alignment
internally and restores that same slot. No unowned symbolic projection
becomes physical configuration. Ordinary Bare reads retain rr; successful
stores clear it; KPT returns actual translation-dependent rr and all receipts.

Original-tier virtual words fund physical/context access and updated-value
restoration through internally constructed closing wands. Exact source
four-word stack algebra gives three saves plus the untouched fourth gap;
all addresses remain modular, with no global stack no-wrap premise. Frame
ownership remains literal rather than reindexed by SP changes.

Config retains the reviewed actual source MENVCFG/ADUE1 specialization,
including the sufficient ADUE1 premise on Bare (without inventing an ADUE
read there). This is a normalized-body rule over the disabled-SIE common
packet. Fetch/decode/cycles, enabled-SIE prologue/migration, full source
capability restoration/function and source entry/boot inhabitation remain
separate. No new camera, frozen-neighbor or umbrella edits.

Strict owner audit: all284 physical-origin declarations in15 modules,
private lookup with setExporting false, all types/opaque bodies/constructors;
only standard propext/Classical.choice/Quot.sound, no unsafe/partial semantic
dependency and zero exclusions. All24 actual-program/modular-stack checks
passed. Implementation/source hashes recorded after final native build.

Manifest:
- PushOffStackAuxDefs
- PushOffStackBareDefs
- PushOffStackBareProofs
- PushOffStackBarePure
- PushOffStackBareResources
- PushOffStackDefs
- PushOffStackFactor
- PushOffStackKptProofs
- PushOffStackLink
- PushOffStackProofs
- PushOffStackPure
- PushOffStackResources
- PushOffStackRules
- PushOffStackSpec
- PushOffStackWordProofs

Evidence under /tmp/xv6-lean-research:
- push-off-stack-native.log (1,121 jobs)
- PushOffStackAudit.lean and push-off-stack-audit.log (284 declarations)
- PushOffStackChecks.lean and push-off-stack-checks.log (24 checks)
- push-off-stack-freeze.sha256 and push-off-stack-source.sha256

Design: docs/design/push-off-stack-boundary.md.
Independent review pending coordinator assignment.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
