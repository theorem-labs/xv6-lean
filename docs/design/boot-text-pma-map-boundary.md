# Combined map, text and PMA boot export

BootTextPmaMapDefs, Spec, Proofs and Link implement all three approved
native contracts. Build: 884 jobs. Strict owner audit: 18 physical
declarations, full type/opaque/constructor dependency closure, standard
three axioms only, no unsafe/partial dependency, zero exclusions. The component
reuses KernelTextBootMap.Installed verbatim: actual image equality, the
fresh map name installed in era.kernelMap, and equality of the other ten
source auxiliary names. No new pure theorem or camera is needed.

The internal public produce rule consumes an existing complete
KernelTextBootMap.resources bundle under actual BootFacts. It frames its
Era.interp, full map authority, persistent map claims, physical text and
identity-tier text. It invokes BootPma.produce_text_retained on precisely
that bundle's retained column, returning the eight persistent PMA cells
and the exact textRetained column (179 remaining registers per hart, exact
sparse memory/timestamp remainder and every other client).

allocate and allocate_frame invoke the existing map/text producer once
and then produce at its returned era and actual boot state. The fresh
name and image facts are outputs of that real allocation. The frame is
returned literally. There is no second machine-era allocation, duplicate
text carve, PMA-cell duplication, supplied static authority or name/image
agreement premise. Actual encodeAll remains a symbolic witness.

This combines the reviewed ArchReset board-PMA client extraction with
BootCarve sparse text persistence and actual KernelMapStatic name
installation. It establishes no S-mode state, source supervisor capability,
physical page table, translation token or whole native function-entry
inhabitation. The source wrapper may consume these resources later.

Only the new BootTextPmaMap prefix, this design and its STATUS are owned.
All signatures remain unchanged after coordinator approval. The private
framing helper keeps the large static predicate out of proofmode reduction;
the public producer instantiates exactly the five actual native columns.
Final independent coordinator review is pending.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
