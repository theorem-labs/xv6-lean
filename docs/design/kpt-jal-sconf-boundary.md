Source-capability JAL boundary

The source disabled JAL rule consumes pcIs plus instruction bytes and the
full supervisor capability. This adapter specializes the destination to x1
and translation tier to full. It retains the general virtual PC and 21-bit
immediate, requires an even target and accepts any free-stack count.

SieOffPacket opens the actual source ownership; full-tier translation
eliminates Bare using its one-shot token law. Configuration follows from
owned source projections and agreement with an explicit same-hart boot-PMA
cell. The source stack, timer, hardware and translation shot are framed
through actual shared fetch/decode/execute/retire/restart. Afterward the
boundary theorem restores the complete source capability at the target,
with RA=PC+4 and the original stack pointer.

Proofs composes the internal KptJal.Spec; the final native Link now
supplies the actual complete JAL rule. No configuration, decoded
success, phase or component-WP premise belongs to the public source Spec.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
