# Exact source RAM and device PMA obligations

The coordinator read source RiscvFetchExec.v88–160 and RiscvExtras.v600–607
at paper pin fa7f0a01c4b40489fac8ad303f079c2dfc7a1476. The independent Sail
agent compared the actual Defs/Spec against those sources before proofs.

Access widths are1..4096. RAM requires base>=0x80000000 and end<=0x88000000;
device accesses require base>=0x02000000 and end<=0x12000000. Every admitted
access must match an actual generated matching_pma_region result. RAM grants
read/write/execute, every atomic operation at widths<=16, both PTE operations,
no plain misalignment exception and reservation support. Devices require
read/write only. All eight RAM and both device clauses are retained.

The generated Lean model uses Nat widths. For admitted accesses this is
equivalent to the source's positive Int widths. The atomic attribute's own
source quantifier also permits negative Int widths; its Lean counterpart
restricts to the generated model's Nat domain. That is a backend domain
restriction, not the positive-access equivalence. No full cross-prover
semantic correspondence theorem is claimed here. Reservability disequality
is stated propositionally rather than the source's Boolean generic_neq.

The proofs cover all admitted addresses and widths in both regions using
actual range_subset arithmetic, and establish the boot table's universal
class obligation. They do not infer permission in table gaps or crossing
region boundaries. Seven public contracts and all96 physical declarations
passed a106-job build and complete standard-axiom/dependency audit, with no
unsafe/partial declaration and zero exclusions. No heap or register ownership
is generated by this pure result.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
