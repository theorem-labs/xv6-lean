# Independent review: actual bitmap and used-block certificates

Reviewer: Codex coordinator. Read the concrete reader certificates, assembly
proofs, abstract collection helpers and complete literal producer. The generic
bitmap definitions and laws were independently reviewed separately.

The ordered input is tied to every actual live inode and indirect block through
the existing22 W3 reader certificates. A mandatory kernel equality connects
that ordered list to the producer's literal. The checked permutation to blocks
47 through982 proves both duplicate freedom and exact membership; it does not
discard duplicates in a host-language set. The permutation and equalities use
ordinary kernel `decide`.

All1024 actual bitmap bytes equal the pinned literals by a separate kernel
certificate. The bit theorem checks all2000 blocks in the source domain; the
final proof establishes exactly metadata-or-used membership. Padding outside
the source domain remains unconstrained, as in the source. The source checker,
its proposition and the exact used set are connected by proved generic laws.

Producer reproduction and negative tests passed independently. Host checks
verify source revision, source blob, image hash and size, but no host assertion
is accepted as a logical premise. Modified literal output must still pass its
Lean equality. Review: PASS for actual W4/W5. Directory/tree validity and full
filesystem initialization remain open, as do the documented generic bitmap
ownership bridges needed by later filesystem proofs.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
