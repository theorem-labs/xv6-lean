Independent review: combined map, text and PMA boot allocation

The coordinator read all four modules. The native Link supplies the actual
existing map/text allocator and PMA producer. A private generic framing
lemma keeps the large static map assertion opaque while preserving its
exact authority, claims, era interpretation and both text predicates.
The same returned era supplies the eight full PMA cells; persistence
consumes those cells once and retains the exact 179-register remainder
for each hart and all other sparse-text boot clients.

The installed-name fact is returned verbatim from the single actual
allocation. No authority, ghost-name agreement or successful execution is
a caller premise. The framed allocation retains the literal caller
assertion. This constructs boot ghost resources, not physical page tables,
supervisor control state or a complete source function-entry capability.

Validation: owner native build completed 884 jobs. The coordinator's fresh
strict audit checked all 18 declarations in four physical modules, with
full type, opaque-body and constructor cones; standard three axioms only,
no unsafe/partial dependency and zero exclusions. Review passed.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
