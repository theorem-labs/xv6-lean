# Disposition of Fable ninth review

Fable returned **PASS as a conditional native rule**, with no soundness
blocker. Its unchanged result is in `fable-kpt-review.md` and
`fable-kpt-result.json`. This disposition was written by Codex after the
review; it does not strengthen Fable's verdict or represent later evidence
as having been reviewed by Claude.

The approved wording is: native supervisor Sv39 `translateAddr` from the
source per-hart residue, plus native private-tree publication as a
conditional resource rule starting from an already-owned user-tier table.
The native links construct their component proofs; this milestone supplies
neither the initial physical tree and era-named ghost tokens nor their
joint resource inhabitation or a boot-reachable publication event.

The explicit scope qualifications remain: the native ambient PMA is
`pmaBoot`; effective privilege is restricted to fetch or MPRV zero; accesses
use the supported-family and permission predicates; and the native rule
generalizes the source's fixed misa and menvcfg conditions. In particular,
`HartSKpt.v:swp_translate_kpt` explicitly requires PBMTE zero and ADUE one
at lines 960–962. Reset menvcfg zero is a future boot-stage obligation,
not evidence that this source theorem covers arbitrary disabled leaves.
Both publication routes retain the source publisher-agent-zero premise.
The invariant namespace is parameterized, with the source-name wrapper
available separately.

The review inspected the actual nine-register partition and restoration,
TLB lookup/hit/miss, raw upper entries and accumulated global bit, A/D
event branches, invariant masking, guarded continuations and physical
publication transformation. Its exposed memory-guard accounting is retained:
noncanonical zero; hit cached/disabled/reread/written zero/zero/one/two;
miss three walk guards followed by the same A/D alternatives. These are
not total machine-step counts. The actual register subevents remain in
the native folds.

Input coverage is recorded precisely. The prompt supplied a 145-file
primary packet plus instructions and guide, totaling 648036 bytes. The
immutable full packet contained 564 selected files, with 560 additional
compiler-import source files available in the snapshot. The transcript
records 214 tool calls: 174 Read, 26 Grep and 14 Glob. Read returned content
on 172 calls covering 169 distinct paths; two requests failed. Complete
file text was returned for 163 distinct paths, counting metadata as well
as source. `fable-kpt-read-coverage.json` preserves exact requested and
returned ranges and verifies returned line text against immutable files.
This is evidence of supplied/returned text, not comprehension or a
complete declaration-cone review. Grep matches and Glob paths are tracked
separately. No builds or delegated agents ran in Claude's session.

Fable identified eight absent Rocq files: CtxValues, KptTree, KMap,
PtAdBits, CommonWalk, PtreeType, HartBarrier and SRegime. It also lacked
publication build/audit logs, the six edge checks and three unspecified
design documents. A separate immutable 35-file supplement has been
prepared under `/tmp/xv6-lean-research/fable-kpt-round9-supplement`, with
full source texts, seven explicitly identified integration designs, the
exact seven reviewed publication files, raw validation evidence and hash
manifest. It has not been submitted to Claude. Its packet SHA-256 is
`700b40d8a220f66cd8c0ca08de9fb604764f6bde5a0ed462323bf6f6cf433eb5`.
The original snapshot is unchanged.

The six edge examples were independently replayed by Codex and passed.
They check raw byte-family membership, nonleaf singleton behavior,
out-of-width extraction, empty slots and inert depth-zero children.
They are local kernel lemmas, not publication or translation inhabitation.
The supplied publication build and full audit remain separately attributed
coordinator/author evidence. Supplying these files addresses availability;
only a subsequent review could close Claude's missing-source inspection
gap.

Resource inhabitation, actual table-building/publication and SATP-switch
execution, translated instruction fetch and `mycpu`, and all six original
whole-system theorem roots remain open. The suggested small inhabitation
exercise is future work and must satisfy the actual tree and ownership
contracts; no witness is inferred from this review.

Subsequent evidence: Fable round ten has now reviewed that immutable
supplement and reaffirmed the conditional PASS. All eight missing-source
inspection gaps are closed; see fable-kpt-supplement-review.md and its
separate disposition. The paragraphs above describe the round-nine handoff.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
