Fable twelfth review disposition

Verdict: PASS for the exact reviewed conditional scope, no soundness blocker.
Claude Code was invoked with claude-fable-5-1 and max effort, session
5a663e65-8e81-416d-b2b5-c8fa0480889b. It completed in 1,438,070 ms over 136
turns. All 365 visible assistant transcript records identify Fable 5.1;
the result also reports Haiku 4.5 usage with 29 output tokens. The evidence
therefore does not claim that Claude Code exclusively used Fable internally.
No agents or compiler tools ran. The immutable result retains full metadata.

The observed tools were 67 Read, 56 Grep and 12 Glob calls. There were 65
successful Read calls across 60 successful paths, with 48 files completely
returned by Read; 85 primary files were also supplied inline. These records
establish returned text and supplied content, not independent compiler
execution or a claim that every declaration was separately analyzed.

Accepted findings: the general native JAL encoder/decoder matches the
actual generated arm order and preserves eager checks. Alignment-derived
encodability, independent fetch halves, universal translation guards,
actual RA/nextPC effects, saved-register/TP results and arbitrary clocks
are correct. Source wrappers preserve the original tier and stack count;
the unopened mycpu body dispatcher uses actual slot admissibility. Every
component Spec is supplied internally by a native Link. Disabled SIE,
x1-only JAL and explicit same-hart boot-PMA remain declared specializations.

The round-eleven source-map gap is resolved for the reviewed addresses.
Both complete KernelInstrs.v and KernelSyms.v were available. Fable reports
a complete symbol-file read, structural inspection of every byte/instruction
chunk boundary and constructor, and inspection of relevant call/mycpu byte
runs. It explicitly did not read every generated literal. The host-side
Rocq-to-Lean conversion is still a provenance/tooling bridge, not a Lean
cross-language equality theorem. Stale upstream prose does not override
the actual symbol definitions.

The third push_off mycpu call at +0x2c is a real omission in the reviewed
two-site producer. Its Fin3 extension and corrected wording are implemented
in acb2531, outside this snapshot; the original two entries remain unchanged.
The complete 24-instruction/60-byte source code producer was also added
there and independently reviewed. Actual Bare JAL and the unopened generic
caller were completed afterward with independent Codex audits and peer
review; Fable's round-twelve PASS does not cover those later implementations.
The boot PMA producer is likewise newer and does not by itself inhabit a
whole source function-entry capability.

One review phrase needs precision: translation receipts are discarded at
some affine source boundaries, but the public JAL source wrappers explicitly
return their code predicate. Function wrappers retain the whole text even
when locally derived fetch windows are not separately returned. The review's
statement that code windows are dropped must not be read as loss of the
public retained code/text resource.

The missing audit/test source evidence is accepted as an evidence-package
improvement. Subsequent snapshots should contain the exact component audit
drivers, compiled edge-test sources and source freeze manifests, along with
logs. Round twelve includes the boundary-check output log but not its test
source, and the decoder's 1,062-second clean build duration is documented
historically rather than independently rerun by Fable. No retrospective
claim of such a run is made.

Full source-entry inhabitation, supervisor boot reachability, physical KPT
construction, enabled-SIE/migration rules, complete push_off and all six
whole-system roots remain open. This is a component review, not full-port
closure or a new trusted assumption.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
