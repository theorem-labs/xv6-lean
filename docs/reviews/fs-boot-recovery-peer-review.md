# Independent current-era recovery bootstrap review

Reviewed all four generic modules and the separate literal FsBootImage leaf,
following the approved recovery and FsBoot source contracts. The native rule
computes the canonical replay map, view and complete header exception set from
the supplied physical disk. It derives all remaining allocation premises from
CovIn and exact HeaderWF; no chosen committed map or Snapshot.OK is assumed.

The actual-era specialization separates the real boot-client resource, consumes
its physical bytes at the same era image name, and returns the unchanged full
Era.interp, all five other client columns, complete filesystem bootstrap output,
exact unused-byte difference and arbitrary frame. The byte camera is structurally
Era.disk, and the other fourteen disk protocol names are retained. No machine
state or physical disk authority is replaced.

The literal leaf proves a clean header from checked image bytes, derives actual
coverage, empty exceptions and committed=raw at every signed block address. Its
native rule requires explicit current-state disk equality with Image.disk.
The full empty exception handle is retained; no seal or buffer invariant is
silently introduced.

A fresh independent audit checked all 88 declarations across the five complete
physical modules, including private helpers, types, opaque bodies and
constructors. Only propext, Classical.choice and Quot.sound occur; zero
exclusions or unsafe/partial/Initial allocation dependency. Evidence:
FsBootRecoveryPeerAudit.lean and fs-boot-recovery-peer-audit.log in
/tmp/xv6-lean-research.

PASS for current-era disk recovery to native byte bootstrap. Generic HeaderWF,
full crash-history resources, configuration and buffer invariants, and crash
preservation remain explicit later obligations.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
