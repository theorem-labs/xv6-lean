# Independent byte-view invariant review

Read all seven modules against FsBlocks.v:289–331,839–905,933–1158,
1618–1705. The actual body preserves its nine source conjuncts and
existential L/C/X with Xv fixed outside. It owns full logged byte authority,
cache halves and exception authority. Cache authority remains outside.
The namespace is the actual child fsbN under parent logN, preserving
source mask premises and sibling opening opportunities.

Signed byte-run lookup and equal-length submap agreement are kernel
proved. Home membership is derived from a nonempty owned run, its
in-block offset and exact byte domain, then cache domain yields the
actual cache entry. Native half agreement and logged byte authority
establish the returned equality. It is not assumed by the caller.

All read crossings open and restore the real invariant. Sealed crossings
use the actual discarded empty exception fragment; unsealed recovery
crossings use the held full handle and explicit outside-set condition,
returning the same handle. All DFrac variants preserve their fractions.
The row/at/any/anyAt wrappers retain the source existential ordering and
seal distinction. No byte allocator, BioView placeholder, opaque invariant
assumption or full filesystem boot claim occurs.

Fresh independent audit passed all93 physical-origin declarations, their
opaque values, types and constructor dependencies; standard three axioms
only, no runtime exclusions, unsafe/partial cone or initial snapshot
allocation call. Command: `PATH=/home/jason/.elan/bin:$PATH python3 tools/lake.py
env lean /tmp/xv6-lean-research/FsBytesInvariantOwnerAudit.lean`.
Evidence: `/tmp/xv6-lean-research/fs-bytes-invariant-peer-audit.log`.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
