# Native filesystem snapshot preparation review

Verdict: PASS for the declared preparation/routing core. Coordinator read all
six modules, actual Defs/Spec, design/STATUS and source FsCfgSnap sections6/9,
with the already reviewed durable clone, recovery and byte-bootstrap contracts.

The loan restores original Pfs through its real durable restoration wand and
returns a separately cloned native instance. Own-state readback identifies
canonical recovery; HeaderWF comes separately from Pfs. Explicit start=2 and
log coverage reconcile the snapshot's superblock geometry and derive metadata
coverage. The three rounded-width binders were corrected to explicit Nat to
match the design/source before proof closure.

Map-to-region routing uses domain containment, preserving arbitrary extra
keys until affine restriction. Native root authority agrees with the real
spare token; the token remains in its keep-alive. Free-node equality derives
from Local.bareFree; live top fragments and complete boot column are disjoint.
Allocation derives root-slack validity from the held native snapshot and uses
those SAME fresh link/top names for actual current-disk boot. Original snapshot,
Pfs, full Era interpretation, all five other clients, exact physical remainder
and arbitrary frame are retained. Full icfg/log/ipool/Bio kits and physical
extent/crash preservation are correctly excluded from this contract.

Owner build passed608 jobs. Coordinator reran the complete physical/type/opaque/
constructor audit; see fs-cfg-snap-root-audit.log. All92 declarations use only
the standard three axioms, zero exclusions and no Initial dependencies. Direct
clone/root-slack/byte-mint inventory matches the actual proof bodies. The source
runtime root-slack use is algebraic allocation from native readback, separate
from the forbidden runtime Initial snapshot constructor.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
