# Independent root review: native link packing and allocation

Verdict: PASS. Codex root read all six FsStateLink modules against the
pinned FsStateInode packing/scattering and FsState gathering/allocation
theorems. Tokenless entries impose no type constraint; total choice functions
are assembled only from the finite map's actual entries. Fixed-name packing
retains the inode authority as its accumulator. Empty maps never manufacture
ownership at a caller-selected name.

The inode and complete ghost factorizations preserve local validity,
marker/count obligations and all actual link resources. Gathering returns
the same-name native element and a valid choice; spare-token validity
retains that extra token in the combined element. These are ownership
transformations, not pure substitutes for the camera.

Fresh allocation uses the existing FsLink and FsTop capacities. Independent
link/top maps are supported with the source validity premise only on the
link family. Root-slack allocation splits the actual combined native element
and returns the spare token. Full-supply allocation is separately justified
by the already proved full-family validity. No Iris world is reallocated;
the boot allocators do not claim to transport a pre-existing filesystem or
allocate durable bytes.

A fresh separate audit passed for all 52 declarations and complete type/body
dependency cones: standard three axioms only, no unsafe/partial dependencies
and zero exclusions. Native durable-byte carving and P_dur remain subsequent
obligations.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
