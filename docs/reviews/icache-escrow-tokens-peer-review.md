# Independent inode escrow-token review

Codex coordinator read all four modules and the complete pinned EscrowDefs.v.
PASS. committedA is the actual mono-nat lower bound one; redemption uses raw
nonunital Excl Unit. Corpse rows retain the full pre-deposit transaction and Qp
share. Native map validity gives exact full-row exclusion and pair agreement;
registry halves split/join with their actual positive fractions. region_pending
retains the source existential half plus committed fragment. Insert/update/delete
rules use real authority and existing full fragments, while fresh ticket/map
allocation preserves arbitrary frames in the same world.

Registry slots 35/36/37 extend LogEpoch's 34 without replacing any earlier
camera; explicit capacities preserve machine, invariant, filesystem and journal
resources, and mono-nat slot 3 is shared at explicit names. Escrow invariants,
parked transaction payloads and global pool assembly are subsequent work.

Fresh independent `tools/lake.py env lean
/tmp/xv6-lean-research/IcacheEscrowTokensOwnerAudit.lean` passed all 241 declarations
by physical origin, traversing types, opaque bodies and constructors. Only the
standard three axioms occur; zero runtime exclusions and no unsafe/partial
logical dependencies.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
