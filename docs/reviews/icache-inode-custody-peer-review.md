# Native inode link and top custody review

Coordinator review: pass for all five IcacheInodeCustody modules, compared
with the full pinned InodeRegion.v:2113–2545 and bare/free-node definitions.
The source multiplicity and directory boundary delta retain exact integer
record equations. Free-node construction preserves the arbitrary Dinode;
its full Node.Bare theorem separately requires zero links, because the source
ireg_bare predicate alone covers size and addresses.

The link predicate owns actual uniform-multiset authority and the root's
separate singleton. Every mutation retains that root token. Zero-count root
retyping/fill is refuted by native validity. Non-root retyping uses the empty
authority's type independence. Native mint/fill/drop transfer the exact
repeated piles, including zero-size piles and arbitrary directory parent
values. The type relation distinguishes directory from nondirectory and
imposes no invented parent constraint.

Root liveness follows from authority and the extra singleton; the held-pile
bound combines that singleton with the caller's distinct pile before reading
native validity. Agreement and live/type readings consume actual fragments.
There is no pure root-allocation assumption and no double-counted token.

The top park holds an arbitrary full native node fragment, guarded by the
source's zero-type implication. At zero type it yields both actual bare
record facts and the exact freeNode; nonzero records impose no invented
payload tie. The existing FsLink/FsTop capacities remain in the same registry;
there is no new slot, allocation or whole-region invariant claim.

A fresh independent full-origin audit checks all 73 logical declarations,
including opaque bodies, types and constructors. Only the standard three
axioms occur, with zero exclusions, unsafe/partial or Initial dependencies.
Evidence: /tmp/xv6-lean-research/icache-inode-custody-peer-audit.log. Epoch,
escrow/registry and full slot protocols remain separate dependencies.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
