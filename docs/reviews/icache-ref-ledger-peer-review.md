# Native inode-reference ledger review

Coordinator review: pass for all seven IcacheRefLedger modules. All definitions,
specifications, algebra, ownership, update, boot and registry proofs were read,
then compared with Xv6Cameras.v 615–712 and IcacheRef.v 333–441, 543–600,
1300–1748 and 1990–2011 at the pinned source revision.

The finite map contains an authoritative element at each signed inode key.
Its three nested products retain typed exclusive claims, additive plain count,
exclusive indexed freeze phase and additive licensed count. Raw optional
exclusive carriers retain invalid values. The concrete NatAdd wrapper has the
source additive operation, zero core and natural inclusion order; it does not
change the existing monotone-counter instances. The ledger is distinct from
the filesystem link multiset and all three coupling maps.

The native validity/inclusion arguments recover complete type, transaction,
share and phase agreement. Claim minting needs an absent cell. Spending needs
the corresponding fragment; freeze rephasing needs the held freeze token.
Both counter updates are actual local updates. The source runit_any remains
plain only. Boot splitting consumes the exact supplied per-key authority and
fragment map; fresh allocation is a separate frame-preserving theorem over
arbitrary signed finite sets. Slot 31 retains the entire earlier registry.

The independent fresh physical-origin audit checked all 308 declarations,
including private helpers, complete types, opaque bodies and constructors.
Only the standard three axioms occur; unsafe/partial dependencies and native
durable initializers are rejected. Zero roots are excluded. Evidence:
IcacheRefLedgerOwnerAudit.lean and icache-ref-ledger-peer-audit.log under
/tmp/xv6-lean-research (the reviewer reran the inspected audit independently).

This closes the source ledger algebra and native allocation slice. Actual
inode-slot reference constraints, record/mirror coupling, transaction shares,
escrow and the full cache bootstrap remain additional obligations.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
