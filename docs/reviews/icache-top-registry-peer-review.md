# Independent native top-registry review

Reviewed all five IcacheTopRegistry modules against the pinned
`iris/InodeRegion.v:3015–3230` and the recorded camera carrier.
The finite arm map stores the full transaction, positive share and inode set;
receipts own full map fragments, while the invariant parks the actual LogTx
share. Freshness comes from the native finite map. The invariant allocator
consumes the supplied empty authority at the same name.

The arm, disarm and release rules preserve the exact clean predicate and
parked resources. Release returns the recorded share. The clean accessor
uses actual empty transaction authority to rule out every parked entry,
returns the top authority while the invariant is open, and supplies the
mask-restoring continuation. It preserves the same empty transaction
authority. There is no pure substitute for the invariant or hidden allocation.

Slot 38 preserves slots 0–37 and 39 upward; explicit capacities connect
the same native top, transaction and machine cameras. The five public
contracts are discharged by the implementation. No full byte/cache/region
invariant, recovery seal or whole-system theorem is claimed.

Independent rerun of the complete physical-origin audit passed all 204
logical declarations, including private helpers, opaque values, declared
types and constructor dependencies. Only propext, Classical.choice and
Quot.sound occur. No unsafe/partial semantic dependencies, excluded runtime
companions or initial snapshot allocators occur.
Command: `PATH=/home/jason/.elan/bin:$PATH python3 tools/lake.py env lean
/tmp/xv6-lean-research/IcacheTopRegistryOwnerAudit.lean`.
Evidence: `/tmp/xv6-lean-research/icache-top-registry-peer-audit.log`.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
