# JAL boot-resource review

Codex independently reviewed `JalBootResources{Defs,Proofs,Map,Code,Share,Link}` and the definitions they consume. The semantic/resource review passes. This is concrete proof-resource bookkeeping for the JAL gate, using the actual native register/TSO resources and `BootFacts`; it does not claim a separate source kernel-allocation theorem or a complete boot handler.

`registers_split` starts with all eight actual initial register maps, applying the checked map split pointwise. Every hart retains its 178 CPU-owned typed cells, and the separate wire bundle contains both full hardware-pin cells on every hart, matching pinned `WireInv.v` (`fa7f0a01c4b40489fac8ad303f079c2dfc7a1476`). No actual register value is replaced by a preferred reset table. `reservations_split` proves equality between the actual finite reservation map and the all-eight-hart set presentation, including keys whose reservation value is `none`; it neither drops an empty reservation cell nor allocates another copy.

`extract_keys` requires a duplicate-free address list, extracts one actual map cell per key, and returns the exact iterated-delete remainder. The proof checks that subsequent keys survive each deletion. The four concrete keys are exactly the modular byte window at the JAL vector, and checked equalities tie their values to `nthByte 0x6f#32`. All four lie in actual RAM. `boot_code_lookup` derives the map lookups from its explicit finite-map decoding equality and the actual `BootFacts` loaded-memory clause. It imposes no preboot-register assumption and does not replace that memory clause with sampled executable tests.

The physical byte resources use the same byte camera and existing era heap name as `Tso.Interp.bootClients`; timestamps use the same timestamp camera/name. `bootTimestamps` supplies exactly `(0, payNone)` at the four existing keys. Only these four full timestamp fragments are converted into persistent pristine fragments. The timestamp remainder retains every other original cell at full ownership. This deliberately makes the JAL instruction window immutable through its timestamp protocol; it does not convert all boot RAM or the physical byte fragments to discarded ownership.

The four physical byte cells are split by three exact native fractional halvings into eight equal positive shares. The type `Qp` guarantees positivity, and the native fractional equality proves that the shares collectively preserve the original ownership. The persistent pristine window is duplicated alongside those byte shares. The finite-set/list conversion uses the actual duplicate-free enumeration of all eight CPUs, so `sharedCode` supplies one complete four-byte/pristine resource bundle per hart. `boot_code` also returns the exact full byte remainder, full timestamp remainder, and initial log-length receipt. Metadata and other era resources are outside this combinator's input and are not claimed to have been allocated or consumed by it.

Validation: the owner's successful 464-job target build was inspected. An independent physical-module-origin audit covered all 49 logical declarations in six modules, including private helpers, with only `propext`, `Classical.choice`, and `Quot.sound`. One compiler-generated total-recursion runtime companion was excluded as an audit root; no unsafe/partial declaration occurs in the logical dependency cone. The main `boot_code` theorem uses the same standard three axioms. Audit: `/tmp/xv6-lean-research/JalBootResourcesIndependentAudit.lean`, log `jal-boot-resources-independent-audit.log`.

Import cleanup identified and independently checked in a read-only copy: `JalBootResourcesProofs` can import `EventWPProofs` and `RegisterProofs` directly instead of `EventWPLink`, supplying `⟨Registers.initialMap_lookup⟩` to the ownership split. This avoids importing the linked native WP implementations merely to obtain a map lookup fact. Probe: `/tmp/xv6-lean-research/JalBootResourcesImportProbe.lean`, log `jal-boot-import-probe.log`. No production source file was edited by this reviewer.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
