# Native RX text resources: independent review

PASS. The coordinator read all seven frozen Lean modules and the source
RiscvPtsto text definition, access, persistence, tier and given-PPN rules;
HartLift2 pristine readback; and InstrBytes window/dispatch definitions.
All 36 approved contracts are constructed by native links.

The byte assertion retains the exact RX map, positive virtual address,
physical text interval, tier pin, actual heap byte and discarded timestamp
(0,None). The physical trampoline page lies inside the exact text interval;
full-tier virtual addresses may map nonidentically. Persistence changes only
the actual byte fraction using native GhostMap persistence. It does not mint
a new timestamp pin or owned context share. Mapping and byte agreement use
the same era's native authority-backed fragments.

Window extraction obtains a PPN from the first actual byte and derives each
same-page PPN by native map agreement. Physical offsets follow checked modular
arithmetic. The same-value closing wand retains the original claims and
pristine resources. Identity extraction works bytewise without a same-page
assumption. The unconditional four-to-two-plus-two split is exact, including
page offset4094; it never assumes the two physical pages are contiguous.
Discarded-context conversions invoke the existing pristine-discard route.

Readback projects actual heap and timestamp validity at the same state,
derives the latest write at time zero, and applies the all-agent/all-view
visible-zero read law. Its pure consequence preserves the original linear
heap/TSO/byte resources for reassembly. The proof uses no arbitrary memory
or successful-translation premise. Source InstrBytes construction, actual
fetch and initial code-map allocation remain separate work.

The owner build passed 687 jobs. A fresh independent audit checked all 150
physical declarations and full types, opaque bodies and constructors:
only propext, Classical.choice and Quot.sound, no unsafe/partial dependencies
and zero exclusions. Evidence: KernelTextDatumRootAudit.lean and
kernel-text-datum-root-audit.log in /tmp/xv6-lean-research.
No production proof was changed during this review.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
