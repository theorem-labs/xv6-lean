# Independent complete shared A/D update review

PASS for the seven KptAD modules. The Codex coordinator reviewed the fixed
Defs/Spec, both complete PTE wrapper folds, guard distribution, all update
branches and native links separately from their implementer. The source
boundary is HartSKpt's kernel leaf read/write and shared A/D composition,
with the actual generated update_and_write_pte retained.

The five control cells are split only to frame MENVCFG around the real
read/write prefixes. Each prefix reaches the exact original request and
continuation; the native shared exclusive or conditional event discharges
the physical boundary. The same fractions reassemble after each wrapper.
The initial contract supplies no physical leaf word or direct slot.

Cached update-none and disabled ADUE consume no memory guard. Enabled
execution selects the actual exclusive-read word, derives its permitted
kernel-leaf A/D variant, performs the actual validation/permission plan,
and follows that word's real update_PTE_Bits result. Reread-nochange returns
the observed word and its retained snapshot reservation. Written returns
the actual new word, cleared reservation and positive authored log/view
receipt. The guard counts are exactly0/0/1/2. Unknown observed values and
their facts are inside the guards. enabled_guards uses additive branching
to obtain the possible continuations; it duplicates no linear client.

The actual false conditional-write and error tails remain in the generated
factorization. The native conditional event and proved permission/configuration
facts discharge these under the stated resources. No supplied success equation
or custom interpretation replaces those obligations.

Target passed794 jobs. The owner and fresh coordinator audits each check
all102 physical declarations, complete types, opaque bodies and constructors:
only propext, Classical.choice and Quot.sound, no exclusions or unsafe/partial
dependencies. Coordinator traversal disables exporting. Evidence:
KptADRootAudit.lean and kpt-ad-root-audit.log under /tmp/xv6-lean-research.
Full hit/miss links are separate composition layers; boot remains open.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
