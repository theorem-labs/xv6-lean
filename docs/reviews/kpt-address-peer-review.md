# Independent complete native residue translation review

PASS for all six KptAddress modules. The coordinator reviewed the approved
six pure, three resource and one native contracts, complete implementations,
resource-return corollary and registry links independently of their author.

The actual residue is opened into its full SATP, TLB and two PMP vector
cells. Five separate auxiliary cells complete the exact nine-cell footprint.
The register-file overlay only describes those owned values; it is not a
machine transition or equality premise about an unowned full register file.
TOR comes from the owned source PMP resource. Native shared map/snapshot
access derives the actual path and every address-specific hardware condition.

Canonicality is decided internally. The noncanonical path executes the
actual five-read fault prefix and restores the original residue and
reservation. The canonical path executes the real seven-read prefix,
actual TLB lookup and complete shared hit/miss translation, then the actual
result/error suffix. There is no caller-supplied translation-body WP,
lookup/residency, Maps, coherence or per-address configuration premise.

Guarded continuations retain all three miss-read guards and exact subsequent
A/D guards. Observed-word/lookup facts stay inside their guards. Register
cells, shared clients, map, receipts and exact branch reservation are rebuilt
before the genuine continuation. Only TLB can change in the exposed residue;
SATP and both PMP vectors retain their original values. The public
resources_close corollary returns the standard existential source residue.

The native 990-job build and separate owner/coordinator full audits pass.
All 230 physical declarations, types, opaque bodies and constructors use
only the three standard axioms, with zero exclusions and no unsafe/partial
dependencies. Coordinator traversal disables exporting. Evidence:
KptAddressRootAudit.lean and kpt-address-root-audit.log under
/tmp/xv6-lean-research.

This is complete native translation under the explicit supported access,
source ambient controls, mapping and published residue. It does not prove
that xv6 boot establishes those resources, execute translated memory/instruction
wrappers, or close a whole-xv6 root.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
