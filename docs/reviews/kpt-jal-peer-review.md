Independent review: native general shared-Sv39 JAL x1

Read all ten modules in full against the actual generated decoder, fetch,
JAL body and native cycle/resource contracts. The generic immediate encoder
preserves signed offsets. Actual instruction ownership supplies PC alignment;
target-even then derives the otherwise necessary low immediate bit. The
whole decoder keeps its preceding extension checks and constructs an owned
finite read plan; no decoder result certificate is a native WP premise.
The actual body reads nextPC/PC, checks the jump and writes nextPC and x1.
SP, pinned TP and every other software register remain unchanged.

Native fetch borrows the exact shared translation packet and retains all
branch-dependent guards and receipts, including two independently mapped
halves across page boundaries. Active execution and the complete cycle
internally compose preparation, retirement, clocks and real restart. The
only WP premise is the genuine returned continuation. The final Link
constructs every approved component contract.

Validation: 1,122-job build; fresh independent strict scan of all 166
physical declarations in ten modules and full type/opaque/constructor cones;
standard three axioms only, zero exclusions and no unsafe/partial dependency.
All fifteen supplied kernel checks passed and were read, covering negative
and odd immediates, word/halfword alignment, cross-page chunks and SP/TP.
Clean decoder compilation costs 1,062 seconds and produces a large proof
artifact; this affects build cost, not the logical trust boundary. Review passed.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
