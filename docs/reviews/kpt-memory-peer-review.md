# Complete native virtual data access: independent review

PASS. The coordinator read all eight frozen Lean modules (1,014 lines),
including the approved public signatures, generated-program factors, native
translation/data composition and both native links. The raw virtual wrappers
retain every translation, EA and memory exception and the successful-false
store result. Their intermediate Except.error branches are retained until
the actual generated outer runner unwraps them.

The proof pays the actual nine owned register cells, deriving hidden SATP,
PMP and TLB facts from the existing residue. Pure footprint widening preserves
all reads and writes and does not invent register ownership. The native
shared translation rule derives the physical mapping and every A/D branch.
Only then do positive virtual ownership and the actual owned ADUE=1 control
eliminate the noncanonical/disabled outcomes and establish successful physical
translation. These are derived facts under the original branch guards.

The physical word is extracted from the supplied virtual word using native
same-map agreement and aligned same-page geometry. Physical PMA/PMP access
is derived from that word's RAM/alignment and the actual owned vectors.
The read/write boundary executes one additional ordinary memory event at
the actual common view. It returns all translation receipts, its own view
receipt and the correct reservation: translation's reservation for loads,
none after stores. The value-polymorphic closing wand rebuilds the original
virtual word with the original tier and context, retaining the old load value
or installing the actual stored value. The same source residue is closed
around the resulting coherent TLB; unrelated controls are preserved.

The public links supply all component proofs natively. No physical word,
page-table witness, hardware grant, successful translation, generated-body
WP or trap-handler WP is a caller premise. The source PMA/effective-address
configuration remains explicit. This is an eight-byte virtual-memory rule,
not a complete instruction, translated mycpu or boot theorem.

The frozen owner build passed 1,013 jobs. A fresh independent physical-module
audit checked all 201 declarations, full types, opaque bodies and constructors:
only propext, Classical.choice and Quot.sound; no unsafe/partial dependencies;
zero exclusions. Evidence: KptMemoryRootAudit.lean and kpt-memory-root-audit.log
under /tmp/xv6-lean-research. No production files were changed by this review.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
