# Independent complete shared translation-miss review

PASS for the seven KptMiss modules. The Codex coordinator reviewed the
approved definitions and five contracts, complete pure/resource proofs,
actual walk/A-D/fill composition and native link separately from their author.

The raw generated continuation is reused without any direct-slot or fixed
upper-flag hypothesis. The walk starts at the supplied snapshot's actual
base and retains its raw G/RSW pointers. Three ordinary shared events select
the cached leaf and actual receipts. Full KptAD then chooses its current
exclusive-read word independently; the implementation never equates the
ordinary read with the current physical value. Branch facts stay inside
the exact subsequent0/0/1/2 guards.

Every successful update response fills with its real selected word:
cached on None, observed after a no-change reread, and new after a write.
The disabled error leaves the original TLB unchanged. Actual fill uses
the installing walk's PPN and global fields; fill_coherent separately proves
those fields agree with the selected word using canonical A/D equality.
It preserves all other entries and returns coherence against the same
persistent snapshot. The six cells are reversibly partitioned and rejoined;
all original clients, three walk receipts and the correct branch reservation
and A/D receipt return to the genuine continuation.

Build passed832 jobs and seven additional kernel branch checks. Owner and
fresh coordinator audits checked all89 physical declarations, full types,
opaque bodies and constructors: standard three axioms only, no exclusions
or unsafe/partial dependencies. Coordinator traversal disables exporting.
Evidence: KptMissRootAudit.lean and kpt-miss-root-audit.log under
/tmp/xv6-lean-research.

Actual lookup dispatch, outer SATP/virtual-address translation, ownership
configuration from the per-hart residue and boot publication remain separate.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
