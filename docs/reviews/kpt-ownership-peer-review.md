# Independent tiered page-table ownership review

PASS for all eight modules. The coordinator read the complete implementation
and compared it with PageGeom54–69 and PtTree395–416,911–913,1150–1518.
The approved geometry and all seventeen spatial/typeclass contracts are
implemented and discharged by the native Link, with no new camera.

The source page lower bound is the imported end_ symbol. Whole-page RAM
coverage and allocation-range validity are separate explicit node facts.
The capacity derives its ghost Views from the same machine. Both tiers own
actual physical bytes: kernel pins retain their original floors/anchors;
user slots retain their context ID. Forgetting derives the actual byte
resources, alignment and both RAM endpoints without assuming memory contents.

The ascending512-index enumeration has checked lookup, uniqueness, length
and list-position correspondence. The generic update cut uses the actual
big-separation deletion law; every other index is proved different and
retained. Page/child accessors retain the node claim and exact remainder.
Both three-level path proofs consume all extracted slots on reassembly.
The update form builds the actual setLeaf description, preserving untouched
upper pages and children. No distinct-page or restoration premise is added.
At depth zero inert children remain in the description and ownership stops.

Recursive IProp definitions are noncomputable, avoiding an unnecessary
compiler runtime companion. This does not change their kernel equations.
All Timeless/Persistent fields are proved from the native cameras and the
depth induction. The registry uses the existing machine and KPT witnesses;
all machine/ghost/view identity theorems are reflexive.

The owner build passed648 jobs. The coordinator independently reran the
complete audit of182 declarations, following types, opaque bodies and
constructors: standard three axioms only, zero exclusions and no
unsafe/partial semantic dependency. Evidence:
/tmp/xv6-lean-research/KptOwnershipOwnerAudit.lean and kpt-ownership-root-audit.log.
Physical allocation, shared invariant publication and native execution are
separate consumers of this ownership. The review used a distinct AI-agent
role; it is not a human review.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
