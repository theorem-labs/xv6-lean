# Independent native lookup and dispatch review

PASS for all five KptTranslate modules. The Codex coordinator reviewed the
approved definitions and contracts, complete pure and native proofs, and
closed Link independently of the author. The Link discharges both full native
hit and miss implementations; there is no supplied translation-body WP.

The generated translate program is definitionally factored into its real TLB
lookup and dispatch. The one-read plan uses the same six owned cells. A hit's
actual selected index and resident entry are derived from the lookup; coherent
provenance and mapping determinism recover its cached leaf and raw upper
pointers. Empty and rejected-tag entries use the complete shared miss rule.

The additive finish conjunction selects an arm without copying linear
resources. The miss preserves three walk guards and their actual receipts;
each arm then retains the exact zero/zero/one/two A/D guards. Lookup and
observed-word facts remain inside those guards. Full cells, clients,
reservation, receipts and snapshot-relative coherence reach the genuine
continuation. Foreign-tag misses are not assumed away.

Native/registry build passes 849 jobs. Owner and fresh coordinator audits
check all 133 physical declarations, complete types, opaque bodies and
constructors, with only propext/Classical.choice/Quot.sound, no exclusions
and no unsafe/partial dependencies. The coordinator disables exporting during
traversal. Evidence is KptTranslateRootAudit.lean and
kpt-translate-root-audit.log under /tmp/xv6-lean-research.

This boundary starts at translate 39. SATP, canonical virtual addresses,
register residue assembly and boot publication remain subsequent layers.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
