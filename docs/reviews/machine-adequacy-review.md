# Native machine adequacy review

Codex independently reviewed `MachineAdequacy{Defs,Proofs,Link}.lean`, its status, the native Iris adequacy theorem, and the allocation, power, language, and observation definitions used by the proof. The review passes for the stated conditional adapter. There is no request to change a proof or its hypotheses. The explicit `BootInitializer` still needs a concrete client proof before this becomes a closed JAL or kernel safety theorem.

`PoolSteps` is an abbreviation for native Iris `Language.NSteps` over the actual `Machine.Step` relation. The initial pool is exactly `[power]`. The adapter passes the supplied observed schedule directly to `wp_strong_adequacy_gen`; it introduces no restricted schedule, replacement transition system, or assumption that the final threads are already reducible. Native adequacy supplies `NotStuck` for every member of the final pool, including forked workers. Since the actual language has `Val = Empty` and `toVal = none`, `safe` eliminates the value alternative and obtains an existential actual `Machine.Step` successor. A trivial postcondition on values therefore does not make the operational conclusion vacuous.

The native theorem is checked at Iris pin `728a17140939e49af9236f7cb0d037da9ec52435`, `Iris/ProgramLogic/Adequacy.lean:174–231`. Its soundness proof allocates the invariant world and soundness credits before invoking the application's universally quantified allocation premise. The adapter binds that supplied `InvGS` and uses it throughout application allocation, the observation invariant, `PowerWP.wp_power`, and the state interpretation. It calls `MachineInterp.initial_off_alloc` directly; it does not call the separate wrapper that allocates native invariant names. `registryPreS` supplies only the existing registry's pre-capacity, not an additional runtime world. The zero additional laters and trivial fork postcondition match the actual `MachineInterp.irisGS` fields. The initial allocation's thread-count argument and the native theorem's initial count differ harmlessly because the supplied state interpretation ignores both thread and step counters.

Initial allocation uses `initial.devices.virtio.v_disk` itself. The disk authority describes that physical disk with its explicit represented size, and the initializer receives full fragments for its actual prefix `[0, diskBytes)`. No zero-filled replacement image is substituted. The adapter does not assert ownership of an unrepresented disk tail or filesystem durability. `initializer_of_handler` explicitly uses Iris affinity to discard those initial fragments when a particular client needs only the shared observation invariant; that specialization does not establish a crash predicate.

`BootInitializer` must work in every supplied native invariant world and at the freshly allocated fixed names and whole trace. Its result is the actual persistent `PowerWP.bootHandler`, which quantifies over every actual `BootFacts` state and finite-map decoding witness and owes the native WPs of all eleven workers. This includes arbitrary preboot register files, repeated boots, both clock choices, and all machine transitions admitted by the unchanged power semantics. The initializer is a substantial explicit client obligation, not an already-proved JAL or xv6 theorem.

For the trace conclusion, the final continuation opens the final state's own `obsInterp`. Its remaining future is empty, so the stored history equals the actual schedule's observation list. The accompanying `ObservationsOK` fact is then returned together with native thread safety. No independent assumption of final trace well-formedness is introduced. Its scope is precisely the existing observation predicate, including alternation, boot count, and the current UART wire tie.

This matches the machine-safety and final-history portion of pinned xv6iris `iris/RiscvAdequacy.v:1298–1617`, especially the observed singleton-power adequacy invocation at line 1512, at commit `fa7f0a01c4b40489fac8ad303f079c2dfc7a1476`. The source's general crash invariant, disk projection/custody/lending interface, arbitrary client trace predicate, and filesystem/kernel conclusions remain outside this adapter. The status accurately distinguishes those obligations from its proven conclusion.

Validation: an independent target build passed all 429 jobs. A fresh physical-module-origin audit checked all **nine** logical declarations in the three modules, including private declarations, and traversed statement and proof dependencies. Only `propext`, `Classical.choice`, and `Quot.sound` occur; no unsafe or partial declaration occurs in the logical dependency cone, and no compiler runtime companions needed exclusion. `registry_safe` has exactly those three axioms. The status's earlier count of eight declarations is stale bookkeeping, not a proof issue. Audit and build records are `/tmp/xv6-lean-research/MachineAdequacyIndependentAudit.lean`, `machine-adequacy-independent-audit.log`, and `machine-adequacy-independent-build.log`. This reviewer changed no production Lean file.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
