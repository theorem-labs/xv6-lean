# Independent actual mycpu boot-resource review

Codex coordinator reviewed all five modules, actual byte and decoder facts,
existing native boot extraction/allocation contracts and source BootCarve,
KMap and KernelText boundaries recorded in the design. PASS. The proof extracts
one unique34-byte span from existing byte and timestamp maps and retains exact
deleted-map remainders. Each actual2/4-byte fetch borrows from that span using
proved bounds and byte equality plus a linear reassembly wand. Matching owned
fractions split both cameras. Source-style physical sharing explicitly persists
timestamps, then bytes, before supplying all overlapping persistent windows.
Discarded timestamp ownership is never promoted to an owned fraction.

The real boot wrapper discharges byte and decode premises from actual boot
facts. It invokes native Era.allocate in the same world, retains the full era
interpretation, every other era client including extracted-key metadata, the
log receipt and AuxiliarySame. The full-memory finite-map witness is symbolic
and never evaluated. The resulting discarded span supports arbitrary supplied
context lists without privileging one hart. No source mapping/tier pin, running
context allocation, full power-loop invariant or function WP is claimed.

Fresh independent `tools/lake.py env lean
/tmp/xv6-lean-research/MycpuBootResourcesAudit.lean` passed all61 logical
declarations by physical origin, traversing types, opaque bodies and constructors;
standard three axioms, zero exclusions and no unsafe/partial logical dependency.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
