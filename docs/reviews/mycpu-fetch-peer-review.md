# Independent actual indexed mycpu fetch review

Codex coordinator read all five MycpuFetch modules and the actual generated
Fetch232–284 and Zca query. PASS. Nineteen actual register reads reuse one
nine-cell footprint; seven PC reads, eager misa.C query and eleven Bare/physical
reads remain explicit. Real indexed two/four alignment, RAM and classification
facts derive from the pinned bytes. No blanket four-byte alignment is assumed.
The result is the exact F_RVC or F_Base encoding at each of fourteen addresses.

FixedRead retains the original arbitrary-response residual. Its immediate
success equality is for the actual owned word only: the second-fetch branch
on another two-byte response remains in the model. This avoids a false generic
single-read claim. The native fold derives the actual word from context
ownership, and the shared corollary retains the persistent34-byte span while
supplying its exact discarded window without changing timestamp fractions.
All nine register cells, running context and chosen-view receipt are preserved.

Fresh independent `tools/lake.py env lean
/tmp/xv6-lean-research/MycpuFetchAudit.lean` passed all83 declarations by physical
origin, traversing types, opaque bodies and constructors, with the standard
three axioms, zero exclusions and no unsafe/partial logical dependency.
Configuration is explicitly Supervisor/Bare with actual permissions; cold boot
has not been claimed to establish it. Dispatch, execution, KPT and full source
function correctness remain subsequent work.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
