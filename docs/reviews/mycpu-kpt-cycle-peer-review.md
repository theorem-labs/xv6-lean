# MycpuKptCycle independent review

PASS for the thirteen pure and two native contracts. Read all eight complete
Lean modules, including all748 lines, and the six owner's kernel boundary
checks. Fresh independent full physical/type/opaque/constructor audit covers
all131 declarations with standard axioms only and zero runtime exclusions or
unsafe/partial dependencies. Owner build1,112 jobs and six checks passed.

Native register plans use only the actual50-cell packet. SATP/TLB and both
PMP vectors remain exclusively inside the coherent KPT residue. Decoder
snapshot certificates become native finite read plans with exact ownership;
they do not replace a successful native decoder WP. Active execution derives
SIE from source off/mstatus ownership and internally performs dispatch,
actual indexed fetch, landing/decoder/setNextPC and the linked actual body.
The public nativeSpec discharges every internal component interface.

The cycle additionally performs actual setup, success suffix, retirement,
optional clock behavior and genuine restart. All fetch/translation/A-D/data
guards retain their branch observations in scope. Register-only and memory
body outcomes preserve exact reservation effects; restart clears it to none.
The fixed save area, code, full GPR/bit/control packet, coherent residue,
running context, all event receipts and arbitrary frame return intact.
Completed admits arbitrary actual clock successors; no counter or mip value
is silently fixed. Return masks the low RA bit.

The only final WP premise is the real returned cycle under its genuine
restart guard. This does not close the14-cycle phase/result proof, source
capability wrapper, boot establishment or any whole-system root.
Evidence: /tmp/xv6-lean-research/MycpuKptCycleRootAudit.lean and
mycpu-kpt-cycle-root-audit.log.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
