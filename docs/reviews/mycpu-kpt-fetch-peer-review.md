# Independent indexed mycpu fetch adapter review

PASS for all sixteen contracts. The coordinator read all six final modules
and the existing actual image/decoder certificates they invoke. Owner build:
1,068 jobs; eight kernel checks passed. A fresh full audit checked all 117 physical declarations through types,
opaque values and constructors: standard three axioms only, no unsafe or
partial dependency and zero exclusions. It is recorded in MycpuKptFetchRootAudit.lean and
mycpu-kpt-fetch-root-audit.log under /tmp/xv6-lean-research.

The fourteen virtual RX/pristine windows supply the actual34-byte fetch
footprint, including the final aligned return fetch's following two bytes.
Every indexed instruction gets the correct InstrBytes shape. Fetch borrows
seven physical cells from the same fifty-cell packet, frames the remaining43,
and opens the four translation cells solely through the native residue.
The full packet, persistent code, actual posttranslation reservation and all
receipts return under the original fetch guards. Source MsFacts supply SXL;
remaining actual owned configuration facts are explicit.

The decoder program equality and checked snapshot result are pure certificates.
The native theorem executes fetch only. It assumes no physical-window,
translation result, decoder-success WP or body WP. Native decoder dispatch and
full-cycle composition remain separate obligations, accurately documented.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
