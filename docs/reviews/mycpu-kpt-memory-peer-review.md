# Independent translated mycpu stack-instruction review

PASS for all fourteen pure and two native contracts. The coordinator read
all eight final modules, including raw generated wrapper factors, register
partitions, guarded folds and paired-word composition. A fresh audit with
exporting disabled checked all178 physical declarations, types, full opaque
bodies and constructor dependencies. Standard three axioms only, no unsafe
or partial dependency and zero exclusions. Full build1,056 jobs.

The bodies are the actual image's C.SDSP/C.LDSP instructions at indices1,2,10,11.
The same fifty-cell packet supplies the five translation controls and the two
operands exactly once; the remaining43 cells, native bit frame and coherent
residue are restored. Source MsFacts derive MPRV/MXR/SXL internally. Native
KptMemory executes the actual transformed virtual access from KernelDatum
word ownership. Raw STORE false and error results remain in the factor;
native ordinary RAM ownership establishes the successful branch. Loads write
only RA or S0; stores preserve the full software map.

The complete translation guards and final data-event guard retain unknown
facts in their proper branches. Actual reservations and all receipts are
returned. The paired rule preserves the unselected real virtual save word
through the native single-word theorem and its literal frame. SP and slot
addresses remain unchanged within these four bodies. A future full-function
composition must retain a fixed save-area address while its separate scalar
SP instructions execute; this component does not silently reindex ownership.

No fetch, decoder or full-cycle proof is claimed. Evidence:
MycpuKptMemoryRootAudit.lean and mycpu-kpt-memory-root-audit.log under
/tmp/xv6-lean-research.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
