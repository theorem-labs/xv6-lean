Independent review: native shared-Sv39 mycpu function

All seven implementation files were read in full against the indexed cycle,
source mycpu specification and actual register/body resource contracts.
The phase relation is constructed internally across fourteen actual cycles;
it is not an input assumption. Entire typed-register equality justifies reuse
of earlier arithmetic projections without importing a Bare execution premise.
Stores establish saved RA/S0 before their loads, and the final result restores
all thirteen callee-saved registers and computes the CPU address from TP.

The finite chain preserves the fixed virtual save area and literal caller
frame, introduces every fetch/body branch, retains ordered receipts, and
quantifies over actual clock successors. The public contract supplies only
the genuine return continuation; the Link discharges all component rules.
The configured witness remains separate from native source-entry inhabitation.

Validation: successful 1,155-job build; fresh independent physical-origin
scan of all 202 declarations in seven modules, following types, opaque values
and constructors. Only propext, Classical.choice and Quot.sound occur;
no unsafe/partial dependency and zero exclusions. Review passed.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
