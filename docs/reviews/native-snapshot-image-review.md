# Native initial-image snapshot review

The coordinator read both concrete image and call-graph audit modules. The
five image theorems instantiate the reviewed initial constructor with the
existing full `Image.snapshot_ok`. The independent flattened-byte theorem
uses actual block lookup and width, so preservation of the original physical
`Image.disk` authority is accompanied by a byte equality argument rather
than inferred from framing.

The audit checks theorem identities and physical module origins, traverses
types and bodies with `allowOpaque := true`, and rejects unclassified callers
of both generic wrappers and literal leaves. It is invoked again after all
project imports in the root audit. A compiled negative runtime-wrapper
fixture belongs to the global regression suite. This is a use-site policy,
not a linear capability or crash-transfer proof.

Review: approved. The agent's eight-declaration full-cone audit and 583-job
leaf build passed; the coordinator integrates and runs the complete project
audit separately. Runtime source-instance transport and full resource
readback remain open.


*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
