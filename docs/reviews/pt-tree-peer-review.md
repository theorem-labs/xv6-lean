# Independent page-table tree review

PASS for the declared pure tree and validation scope. The coordinator read
all eleven modules, including the complete proofs and examples, and compared
the representation, shallow mapping/blocking predicates, leaf update and
fixed-depth canonicalization with PtreeType.v and PtTree.v at the pinned
paper source. No correction was required.

The inert carrier has total raw entries and optional children without a
hidden well-formedness premise. Mapping keeps arbitrary upper words; blocking
has exactly the source's three invalid-stop cases. Canonicalization retains
upper words and even inert children below level zero. No claim of physical
ownership follows from these pure definitions.

Validation uses the actual generated five-read program. Outcome quantifies
over every dependent register file and requires a finite checked run; result
uniqueness is proved for arbitrary fuels. The Boolean transcription is tied
to the entire generated free tree. Valid pointer fields are derived from
semantic validity, then used to prove a register plan with all five universal
reads and no owned footprint. G and low RSW remain unrestricted. The concrete
0x321 pointer example checks that distinction. The cross-prover relationship
to Rocq exec is still a separate obligation.

A/D variance proves equality of the complete validation programs for leaves,
including independent eager read responses. Geometry checks the complete
27-bit index decomposition and 44+9+3 address construction. Mapping update
proofs inspect each possible shared prefix, retain untouched children and
words, and derive the distinct final index when required. Canonical mapping
inversion and leaf replacement preserve the original tree's exact bases and
child structure. No success assumption replaces these proofs.

The owner build passed 339 jobs. The coordinator independently reran the
physical-origin audit over all 640 declarations in eleven modules, including
types, opaque bodies and datatype constructors. Only propext, Classical.choice
and Quot.sound occur, with zero exclusions and no unsafe/partial semantic
dependencies. Evidence: /tmp/xv6-lean-research/PtTreeOwnerAudit.lean and
pt-tree-root-audit.log. Native shared ownership, publication and a concurrent
shared-tree translation rule remain separate work.

This review used distinct implementation and review AI-agent roles; it is
not a human review.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
