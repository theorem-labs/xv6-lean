# Independent native RegimeFetch review

PASS; no correction requested. Codex `artifact_audit` independently read the six root-authored modules (Defs, Spec, BareProofs, KptProofs, Proofs and Link), final STATUS/design, both actual native packet partitions and their configuration projections, and the reused guard/trace definitions and monotonicity proofs. This extends the earlier two-file signature review to the complete native implementation. All frozen lengths and hashes remain unchanged.

The public rule uses the exact fifty-cell packet at `sourceShares`, original instruction tier and actual Bare/KPT regime. Config remains the explicit existing source-specialized KptJal.Config, including boot PMA and the other stated control facts; the wrapper does not derive source hardware initialization. Code is actual `KptFetch.instrBytes` at the supplied tier, with no physical word or fetch-success premise. Both F_Base and F_RVC classifications are supported by ownership; error-result code predicates remain false. The same generation certificate, image, fixed names, whole observation trace and hart flow through both native branches.

In Bare, `BareJal.partition` opens the owned existential SATP/PMP values. The synthetic entry file patches only those projections and supplies nine fetch cells out of the fifty-three cells including the Bare slot. Forty-four other cells, the native bit ties and x0 fact remain in the saved frame. MS facts come from that actual frame, and `fetch_config` combines them with the owned mode/TOR facts. Full BareFetch performs the actual byte-backed fetch. Reversing the same partition restores the original fifty-cell packet and owned Bare slot; no equality is asserted for unowned fields of the symbolic control file. Running context is used and returned by the primitive, reservation ownership is framed unchanged, and original code/read receipts/literal frame return.

In KPT, `MycpuKptFetch.packet_partition` supplies seven common fetch cells, forty-three remaining cells with bit/x0 facts, and the separately owned four-cell residue. The existing configuration projection uses packet MS facts and the explicit Config. Actual KptFetch handles translation, hit/miss/A-D behavior and the physical instruction read. The returned cells and updated coherent residue reconstruct the same packet at the same namespace/root. Running context and the literal frame remain outside that operation. The reservation is precisely `traceReservation rr trace`, a left fold of the actual translation outcomes; it is not asserted unchanged or unconditionally cleared. Receipts retain both translation outcomes and actual read views.

The regime-indexed Trace type prevents interpreting Bare read views as KPT outcomes. Bare guards retain one later and quantified view per actual chunk. KPT guards retain the additive hit/miss choice, three miss-read guards, the appropriate A/D guards and the final read guard, with observed facts inside their original scopes. `guards_mono` delegates to the existing linear fold maps and does not duplicate its mapping resource across executable branches. Original code tier is preserved throughout; Admits eliminates only the unsupported Bare/full pairing. No extra source-tier receipt or physical mapping authority is fabricated.

Independent validation: fresh build passed 1,163 jobs. Strict audit passed all 47 physical-origin declarations with private lookup (`setExporting false`), recursive types, opaque bodies (`allowOpaque := true`) and constructor traversal. Only propext, Classical.choice and Quot.sound occur; no unsafe/partial dependency and zero exclusions. All six SHA-256 hashes and byte lengths match the frozen receipt before and after review. No new trivial boundary tests were added: this layer composes the already reviewed native primitives and exact resource partitions, and the review found no unresolved behavior needing an additional fixture.

Evidence under `/tmp/xv6-lean-research`: `RegimeFetchPeerAudit.lean`, `regime-fetch-peer-{build,audit}.log`, `regime-fetch-peer-results.json`, and `regime-fetch-freeze.json`. No implementation file was edited.

The native Link closes both approved contracts. The public WP input is the genuine guarded continuation after fetch. This milestone does not decode, prepare nextPC, execute or retire an instruction, prove a complete function, initialize source capabilities, or establish boot reachability.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
