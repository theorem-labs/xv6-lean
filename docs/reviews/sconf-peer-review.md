# Independent source supervisor configuration review

PASS for all three pure and fourteen native resource contracts. The coordinator
read all four final modules against the source IntrDefs.v595–667, the native
SupervisorBits ownership and HardwareConfig definitions. A fresh audit checked
all63 physical declarations, types, full opaque values and constructor cones:
standard three axioms only, no unsafe/partial dependency, zero exclusions.
Full build893 jobs. Evidence: SconfRootAudit.lean and sconf-root-audit.log in
/tmp/xv6-lean-research.

The hardware bundle contains all eight frozen cells, static claims and the
real generation certificate. Source minstret_inv is exactly emp. Privilege,
mie, mideleg and MENVCFG remain full actual cells, with all source mask and
environment facts and exact literals. The existing native mstatus/ghost-bit
ownership supplies all ten facts and SIE/SRET agreement. sconfAt is the real
replacement accessor, accepting an arbitrary replacement msOwn resource;
its closing wand reuses all remaining actual resources. Interrupt/environment
accessors borrow and reassemble the same held configuration.

This is source resource assembly and access. It does not establish boot
allocation, CSR execution, an enabled handler or whole-function correctness.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
