# Independent actual Sail data-access review

Reviewer: coordinator OpenAI Codex, independently reviewing the artifact agent's seven frozen SpinlockAccess modules. Result: **PASS for the stated instruction-plan contracts**.

Read all definitions and proofs, including the physical and virtual wrappers. The static snapshot constrains only seven actual boot fields; x10 is constrained separately for address transformation, and PMP-off is a separate universal premise. The snapshot certificates use ordinary kernel-checked equality proofs, with no executable evaluator trusted as an oracle.

The request metadata agrees with the generated four-byte accesses. AMOSWAP.W.aq has an exclusive acquire read followed by a distinct normal-strength conditional write. Its plan retains both generated x15 reads and writes the old word back with the actual sign extension. Every old value and permitted protocol successor remains quantified. Reservation equality and the strict size bound are in the reserved write mode; mode eligibility remains an explicit resource-callback obligation. The final reservation is none only after a successful write event. The native EventPlan fold separately handles blocked events.

LW and SW include address transformation, actual M-mode translation, alignment/page splitting, PMA/PMP and non-MMIO checks. Full-word extraction and assembly are proved for arbitrary words. The unlock path reads architectural x0 as zero. FENCE rw,w retains its real barrier event and is correctly identified as non-draining. Five definitional equalities connect these plans to the actual decoded image instructions.

Fresh coordinator audit of all 233 declarations in the seven physical modules and their complete type/body dependency cones passed with only propext, Classical.choice and Quot.sound; zero exclusions and no unsafe/partial dependency. Evidence: spinlock-access-root-audit.log in the research directory. The owner's target build passed 425 jobs.

These are conditional compositional plans, not a proof of lock ownership, PC reachability, pool coverage or the complete two-hart gate. Those downstream obligations are stated explicitly rather than supplied as hidden machine-execution assumptions.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
