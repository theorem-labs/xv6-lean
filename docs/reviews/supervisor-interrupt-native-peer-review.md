# Independent native supervisor interrupt review

Codex independently reviewed the coordinator-authored, frozen `MachCSL/Logic/SupervisorInterrupt{Defs,Spec,Proofs,Link}.lean` against pinned `iris/SmodeCore.v:65–154` and `1197–1224`, the actual generated interrupt functions, and the native `RegisterPlan` fold and single-register WP rules. No correction was required for the declared resource slice.

The native `registers` predicate owns exactly four cells: `misa`, `mstatus`, `mie`, and `mideleg`, each with its own arbitrary `DFrac`. Its existential register file supplies values only for these listed cells, accompanied by the previously proved `Disabled` pure predicate. The footprint has distinct register keys. It does not secretly require the other generated registers, physical pin ownership, `mip` ownership, or a full reset-register file. This matches the four fractional cells in source `dispatchInterrupt_none_S_from_regs`.

The plan preserves all ten generated reads: `misa`, `mideleg`, `mip`, `sig_meip`, `misa`, `sig_seip`, `mie`, `mie`, `mstatus`, `mstatus`. Both status reads remain despite the false Supervisor-versus-Machine comparison in the eager generated expression. The two pins and `mip` use `RegisterPlan.Plan.readAny`; each continuation covers every value of the actual dependent register type. In particular, the unowned `mip` read is not equated with the existential symbolic file's `mip` field. Its output can be arbitrary because the delegation-mask proof eliminates machine-destined pending bits for every word, while disabled SIE suppresses the supervisor branch.

`currentlyEnabled_s_plan` derives the actual Ext_S result from only `misa.S = 1` and the fixed generated S/Zicsr support. No complete `misa` constant or external execution result is assumed. `external_plan` follows the actual MEI/SEI construction and independent universal pin branches. `getPendingSet_plan` discharges both suppression cases, and `dispatch_plan` follows the resulting `none` without entering interrupt priority selection or a trap handler.

`wp_dispatch` folds this checked finite plan through `RegisterPlan.fold`, which opens each selected register cell through the existing native read accessor, uses `RegisterWP.wp_read` with its guarded continuation, and restores that cell and the exact footprint. Unowned reads use `wp_read_any`, whose continuation covers the actual value and whose proof handles both live-generation and older-dead-generation machine steps. The fold preserves the actual machine state interpretation and observation state at each read. No callback is asked to assume successor preservation or a callee WP for dispatch.

At the terminal plan node, the result is exactly `none` and the symbolic file is unchanged. The proof reintroduces the same `registers` predicate, with the same gamma and four shares, and passes it to the caller's genuine `threadWP` continuation. The generation certificate is the existing persistent certificate; no new era or ghost name is allocated. `actual` and `nativeSpec` discharge the complete local specification directly for the supplied genuine native capacities and `InvGS_gen`, with no extra implementation-spec premise.

The native statement remains an adaptation of the source register lemma, which states pure `exec` success under `mstate_interp` and four cells. Here it is an actual event-by-event `NotStuck` WP composition in the existing machine interpretation. The predicate is intentionally only the four-cell resource slice. Full `sconf`, SIE ghost ownership, privilege, clock assumptions, translation resources and a complete supervisor instruction theorem remain outside it and can frame around this rule. The explicit Supervisor argument does not assert the current-privilege register is owned or equals Supervisor. Unchanged symbolic registers do not prohibit asynchronous physical pin changes between reads.

Independent validation: `PATH=/data/jason/.elan/bin:$PATH python3 tools/lake.py build MachCSL.Logic.SupervisorInterruptLink` passes all 436 jobs. A fresh physical-origin audit checked all 45 declarations in the four modules, including private helpers and complete opaque-body/type/constructor traversal via `info.value? (allowOpaque := true)`, with zero exclusions. Only `propext`, `Classical.choice` and `Quot.sound` occur. There is no unsafe or partial semantic dependency, new axiom, `sorry`, native decision procedure or caller-supplied preservation oracle.

Evidence: `/tmp/xv6-lean-research/SupervisorInterruptNativePeerAudit.lean`, `supervisor-interrupt-native-peer-audit.log`, and `supervisor-interrupt-native-peer-build.log`. This is independent review of the coordinator's native layer; this reviewer authored the earlier machine-level interrupt plan that it adapts. The source Rocq replay review is recorded separately.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
