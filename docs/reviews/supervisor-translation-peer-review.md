# Independent supervisor translation resource review

Verdict: PASS for all eighteen contracts. The coordinator read all four final
modules against IntrDefs.v's pending/shot/on and strans_inv definitions,
conflict, flip and arm accessors. The fresh exporting-disabled audit checked
all60 physical declarations through full types, opaque values and constructors:
standard three axioms only, no unsafe/partial dependencies, zero exclusions.
Build670 jobs. Audit evidence: SupervisorTranslationRootAudit.lean and
supervisor-translation-root-audit.log under /tmp/xv6-lean-research.

The existing machine MonoNat camera and era.supervisorTranslation CPU names
are reused. Two half authorities at zero combine and update to full authority
at one plus a persistent lower bound. Conflicts use actual camera validity.
Allocation returns a fresh name rather than claiming allocation at an already
supplied era name. Bare owns SATP, the exact PMP configuration and stvec;
KPT owns the actual four-cell residue and the shot authority. The KPT accessor
borrows and reassembles its residue with a real wand. No hardware mode switch,
TLB flush, boot installation or complete capability follows solely from the
one-shot update, and the component does not claim those results.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
