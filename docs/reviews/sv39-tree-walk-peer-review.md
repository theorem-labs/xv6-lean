# Independent arbitrary-pointer Sv39 walk review

PASS for all six modules in their final layout. The coordinator read the
complete definitions, signatures, factors and native proofs, checking them
against the previously reviewed source tree and generated walk semantics.
The geometry helper was moved into Defs before the final build/audit; no
semantic correction was required.

The pointer theorem reads the entire supplied raw nonleaf word. Actual
semantic Valid/Pointer supplies the universal five-read validator plan.
The exact 64-bit PTE's PPN drives recursion, and its G bit is ORed into the
accumulated global flag. No flag-one pointer rule is called or substituted;
low RSW and upper raw contents remain intact. The whole-tree rule obtains
these facts from the real Maps premise and begins at the tree's base.

The final leaf uses only the geometry fields of the existing Path record.
Its fixed-pointer reference function is never used for upper slots. Actual
read A/D bits are independent of the inert reference and owned physical byte
functions. The returned global bit is the source three-word accumulation;
the kernel permission leaf's own G bit is proved zero. All three original
slots, four register cells, credential, reservation and view receipts return
to the genuine continuation under exactly three memory-event guards.

The shared factor retains read failures and all validator/recursive branches
before specialization. The empty-footprint fold quantifies over actual
register responses; it does not erase eager events. Neither child geometry
nor a ghost tree is used as an alternate memory interpreter.

The final target build passed 647 jobs. The coordinator independently reran
the full physical-origin audit over all49 declarations, including types,
opaque bodies and constructors: standard three axioms only, zero exclusions,
no unsafe/partial semantic dependency. Evidence:
/tmp/xv6-lean-research/Sv39TreeWalkAudit.lean and sv39-tree-walk-root-audit.log.
Shared invariant access, TLB-hit coherence and full virtual translation remain
separate. This is a review by a distinct AI-agent role, not a human review.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
