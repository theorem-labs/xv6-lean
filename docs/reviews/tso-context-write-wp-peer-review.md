# Registered-context word write WP review

Coordinator review: pass for the four TsoContextWriteWP modules. The reviewer
read their complete contents, the actual MemoryWriteWP successor and guarded
node rule, and the pinned HartEvents.v 299–375 and TsoCtx.v 2572–2592 boundaries.

The public rule takes the real generation certificate, running context, full
old physical word and arbitrary reservation fragment. Its request is a present,
nonexclusive eight-byte RAM event. The implementation supplies the entire
resource update internally, using the checked registered context store and
proving the actual successor's one authored log append, byte overlay and
unchanged views. The complete heap metadata, TSO authority, register/device
bundle and context ownership are preserved or updated at their proper names.

The update occurs after the successful event's later and returns the actual
bundle before the lower-level rule clears the reservation and obtains the
unchanged-view receipt. Blocked events keep the original state, expression,
reservation and unopened callback through the native guarded retry. There is
no disjoint-reservation hypothesis, success/fairness assumption, pristine-old
conversion or caller-supplied resource oracle. The builtin bridge retains the
present-value requirement and the dependent event index; other request fields
are unchanged. The client still proves the genuine residual continuation WP.

A fresh independent audit checked all 34 logical declarations, their full
types, opaque bodies and constructors. Only propext, Classical.choice and
Quot.sound occur; there are no unsafe/partial dependencies and zero excluded
roots. Evidence: TsoContextWriteWPAudit.lean and
tso-context-write-wp-peer-audit.log under /tmp/xv6-lean-research.

Virtual translation, tier ownership, complete store instructions and the
mycpu function remain additional obligations. This is a memory-event WP.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
