# Registered-pin store independent review

PASS for the source resource-update boundary. The root reviewer read all
five physical modules, STATUS, the existing transition and timestamp
interpretations, and the exact source map/window statements at
`TsoCtx.v:3596–3620,4190–4274`.

The public map gate retains source equal domains, new-byte membership,
actual authored log append, image/memory equations and monotone bounded
views. The proof derives registered timestamp lookup from the owned full
fragments; this is not an additional assumption. Native bulk updates retain
full heap metadata and every untouched timestamp payload. The semantic
proof uses the complete framed-payload append law, not just the byte-pin
arm. The window adapter retains modular addresses and source injection
bound, with the actual per-offset/address floor and set equalities.

The outputs preserve the same pin floors and allowed sets, expose the new
one-based timestamp and return the exact zero-based authored receipt.
Source existential postconditions follow by the two proved forgetting
lemmas. All subordinate history, monotone-length and view contracts are
discharged by native camera proofs. The Link checks the same existing
machine heap and TSO capacities; no new resource slot is allocated.

This review approves the basic-update resource rule. Actual Sail event
success, reservation handling and conditional PTE write WP remain separate
obligations; the rule assumes only the explicit physical-state transition,
and does not claim to prove that transition.

A fresh root audit `TsoPinnedStorePeerAudit.lean` checked all **43 logical
declarations** in the five physical modules, including transitive types,
opaque bodies and constructors. Only the standard three axioms occur;
zero exclusions and no unsafe/partial or initial-snapshot allocation cone.
Evidence: `/tmp/xv6-lean-research/tso-pinned-store-peer-audit.log`.

*Authorship note: this was researched and written by an AI coding agent
(OpenAI Codex), working on Jason Gross's behalf; Jason reviews what is
posted from this account.*
